package model;

/**
 * State represents the possible health states of an individual in an infectious disease model.
 */
public enum State 
{
    SUSCEPTIBLE, 
    EXPOSED,     
    INFECTED,    
    RECOVERED  
}
